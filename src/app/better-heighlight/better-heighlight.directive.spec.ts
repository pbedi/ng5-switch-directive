import { BetterHeighlightDirective } from './better-heighlight.directive';

describe('BetterHeighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHeighlightDirective();
    expect(directive).toBeTruthy();
  });
});
