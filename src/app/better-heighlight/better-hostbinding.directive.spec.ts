import { BetterHostbindingDirective } from './better-hostbinding.directive';

describe('BetterHostbindingDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHostbindingDirective();
    expect(directive).toBeTruthy();
  });
});
